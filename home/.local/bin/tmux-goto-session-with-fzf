#!/bin/bash

current_session=$(tmux display-message -p '#S')
selected_session_name=$(tmux ls -F '#{session_name}' | grep -v "^${current_session}$" | fzf)

if [ -z "$selected_session_name" ]; then
    exit 0
fi

tmux switch-client -t "$selected_session_name"

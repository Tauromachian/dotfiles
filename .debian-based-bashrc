export PATH=$PATH:/home/jose/.config/php

apt() { 
  command nala "$@"
}
sudo() {
  if [ "$1" = "apt" ]; then
    shift
    command sudo nala "$@"
  else
    command sudo "$@"
  fi
}

eval "$(starship init bash)"
